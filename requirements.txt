# Core ML stack
torch>=2.2
numpy>=1.26,<3.0
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
tqdm>=4.65

# Single-cell
anndata>=0.10
scanpy>=1.10

# Lightning wrapper (model.py supports running without it)
pytorch-lightning>=2.0

# cell-eval — install per Arc Institute's release instructions.
# If pip-installable, uncomment:
# cell-eval

# Optional: ESM2 / scGPT clients the agent may pull during the loop.
# The agent decides what to install; this file only pins the always-on stack.
